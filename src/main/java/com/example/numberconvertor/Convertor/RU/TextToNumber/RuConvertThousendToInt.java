package com.example.numberconvertor.Convertor.RU.TextToNumber;

import com.example.numberconvertor.Convertor.Interfaces.IStringToNumber;

public class RuConvertThousendToInt implements IStringToNumber {
    public int StringToNumber(String textNumber) {
/*        RuDicrionaryContainer dictionary = new RuDicrionaryContainer();
        foreach(var i in dictionary.thousands)
        {
            if (i.Value == textNumber) {
                return 1000;
            }
        }*/
        return 0;
    }
}
