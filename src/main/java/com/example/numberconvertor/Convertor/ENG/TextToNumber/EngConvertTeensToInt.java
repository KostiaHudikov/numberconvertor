package com.example.numberconvertor.Convertor.ENG.TextToNumber;

import com.example.numberconvertor.Convertor.Interfaces.IStringToNumber;

public class EngConvertTeensToInt implements IStringToNumber {
    public int StringToNumber(String textNumber) {
/*            EngDicrionaryContainer dictionary = new EngDicrionaryContainer();
            foreach(var i in dictionary.teens)
            {
                if (i.Value == textNumber) {
                    return Convert.ToInt32("1" + i.Key.ToString());
                }
            }*/
        return 0;
    }
}

